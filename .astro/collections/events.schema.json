{
  "$ref": "#/definitions/events",
  "definitions": {
    "events": {
      "type": "object",
      "properties": {
        "title": {
          "type": "string"
        },
        "tagline": {
          "type": "string"
        },
        "date": {
          "anyOf": [
            {
              "type": "string",
              "format": "date-time"
            },
            {
              "type": "string",
              "format": "date"
            },
            {
              "type": "integer",
              "format": "unix-time"
            }
          ]
        },
        "endDate": {
          "anyOf": [
            {
              "type": "string",
              "format": "date-time"
            },
            {
              "type": "string",
              "format": "date"
            },
            {
              "type": "integer",
              "format": "unix-time"
            }
          ]
        },
        "venue": {
          "type": "string"
        },
        "type": {
          "type": "string",
          "enum": [
            "workshop",
            "school",
            "event",
            "conference",
            "seminar"
          ]
        },
        "role": {
          "type": "string"
        },
        "status": {
          "type": "string",
          "enum": [
            "upcoming",
            "archived"
          ]
        },
        "tags": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "thumbnail": {
          "type": "string"
        },
        "externalUrl": {
          "type": "string"
        },
        "nav": {
          "type": "string"
        },
        "order": {
          "type": "number"
        },
        "parentSlug": {
          "type": "string"
        },
        "$schema": {
          "type": "string"
        }
      },
      "required": [
        "title",
        "tagline",
        "date",
        "endDate",
        "venue",
        "type",
        "role",
        "status"
      ],
      "additionalProperties": false
    }
  },
  "$schema": "http://json-schema.org/draft-07/schema#"
}